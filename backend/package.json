{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "serve": "bun run --watch src/server.ts",
    "build": "bun build --target node16 --outdir dist",
    "lint": "bunx eslint .",
    "test": "bunx vitest run"
  }
}
